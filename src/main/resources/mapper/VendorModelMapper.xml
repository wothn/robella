<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.elmo.robella.mapper.VendorModelMapper">

    <select id="findByModelId" resultType="org.elmo.robella.model.entity.VendorModel">
        SELECT * FROM vendor_model WHERE model_id = #{modelId} ORDER BY id
    </select>

    <select id="findByModelIdAndEnabledTrue" resultType="org.elmo.robella.model.entity.VendorModel">
        SELECT * FROM vendor_model WHERE model_id = #{modelId} AND enabled = true ORDER BY id
    </select>

    <select id="findByModelKeyAndEnabledTrue" resultType="org.elmo.robella.model.entity.VendorModel">
        SELECT * FROM vendor_model WHERE model_key = #{modelKey} AND enabled = true ORDER BY id
    </select>

    <select id="findByProviderId" resultType="org.elmo.robella.model.entity.VendorModel">
        SELECT * FROM vendor_model WHERE provider_id = #{providerId} ORDER BY id
    </select>

    <select id="findByModelIdAndProviderId" resultType="org.elmo.robella.model.entity.VendorModel">
        SELECT * FROM vendor_model WHERE model_id = #{modelId} AND provider_id = #{providerId} ORDER BY id
    </select>

    <select id="findByModelIdAndProviderIdAndEnabledTrue" resultType="org.elmo.robella.model.entity.VendorModel">
        SELECT * FROM vendor_model WHERE model_id = #{modelId} AND provider_id = #{providerId} AND enabled = true ORDER BY id
    </select>

    <select id="findByEnabledTrue" resultType="org.elmo.robella.model.entity.VendorModel">
        SELECT * FROM vendor_model WHERE enabled = true ORDER BY id
    </select>

    <select id="findByModelIdAndProviderIdAndVendorModelName" resultType="org.elmo.robella.model.entity.VendorModel">
        SELECT * FROM vendor_model WHERE model_id = #{modelId} AND provider_id = #{providerId} AND vendor_model_name = #{vendorModelName} ORDER BY id
    </select>

    <select id="findByVendorModelName" resultType="org.elmo.robella.model.entity.VendorModel">
        SELECT * FROM vendor_model WHERE vendor_model_name = #{vendorModelName} ORDER BY id
    </select>

    <select id="findByVendorModelKey" resultType="org.elmo.robella.model.entity.VendorModel">
        SELECT * FROM vendor_model WHERE vendor_model_key = #{vendorModelKey} ORDER BY id
    </select>

    <select id="findByVendorModelKeyAndEnabledTrue" resultType="org.elmo.robella.model.entity.VendorModel">
        SELECT * FROM vendor_model WHERE vendor_model_key = #{vendorModelKey} AND enabled = true ORDER BY id
    </select>

</mapper>