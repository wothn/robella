<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.elmo.robella.mapper.ModelMapper">

    <select id="findByPublishedTrue" resultType="org.elmo.robella.model.entity.Model">
        SELECT * FROM model WHERE published = true ORDER BY id
    </select>
    
    <select id="searchModels" resultType="org.elmo.robella.model.entity.Model">
        SELECT * FROM model 
        WHERE name LIKE CONCAT('%', #{keyword}, '%')
        OR model_key LIKE CONCAT('%', #{keyword}, '%')
        OR description LIKE CONCAT('%', #{keyword}, '%')
        ORDER BY id
    </select>
    
    
    <select id="findByCapability" resultType="org.elmo.robella.model.entity.Model">
        SELECT * FROM model WHERE capabilities LIKE CONCAT('%', #{capability}, '%') ORDER BY id
    </select>
    
    <select id="findByModelId" resultType="org.elmo.robella.model.entity.Model">
        SELECT * FROM model WHERE id = #{modelId}
    </select>

</mapper>